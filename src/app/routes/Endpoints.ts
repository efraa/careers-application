const candidates = {
  resource: '/candidates',
  collection: '/',
  queues: '/:candidateId/queues',
  document: '/:candidateId',
}

const recruiters = {
  resource: '/recruiters',
  collection: '/',
}

export const Endpoints = {
  candidates,
  recruiters,
}
